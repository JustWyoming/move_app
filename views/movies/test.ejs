with the app.get for the movies results by id page

db.MoveDb.count({where:{imdbCode:data.mdbID}}).then(function(foundItemCount){
	res.send("",foundItemCount)
	var wasFound = foundItemCount > 0;
	res.render('movieShow', {moviefound: wasFound, item:movieItem, previousSearch:req.query.lastSearch || false}) << this is part of the back button
})

add the movieFound to the html and a disabled label with an if else statement


need to add a back button breadcrumb and an 'in list' feature to movies in our db


OVERALL THEME IS YOU NEED TO SEND THE IMDB INFO WITH THE RENDER AND PUT AN IF ELSE STATEMENT ON YOUR HTML WITH A DISABLE FIELD

